import { Channel } from "../../domain/channel"

type UsersProps = {
    channel: Channel
}

export function Users({ channel }: UsersProps) {

    return (
        <div>
            USERS
        </div>
    )
}